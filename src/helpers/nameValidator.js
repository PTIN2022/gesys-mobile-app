//Esta función es un helper que comprueba que el valor  de nombre introducido por el usuario es correcto. 
export function nameValidator(name) {
  if (!name) return "El nombre no puede estar vacío."
  return ''
}
